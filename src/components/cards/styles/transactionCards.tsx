import { StyleSheet } from "react-native";

export const transactionStyles = StyleSheet.create({ 
    transImg:{
        width: 8,
        height: 8,
        resizeMode:'cover',
        top: 2,
    },
});